version: 1
frontend:
  phases:
    # Static site — no build step. Files are served as-is.
    build:
      commands: []
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
